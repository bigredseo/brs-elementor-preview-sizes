# BRS Elementor Preview Sizes

A tiny WordPress plugin that changes the default visual preview widths inside the Elementor editor.

## Development

This repository contains the source code for the `brs-elementor-preview-sizes` WordPress plugin.

Development dependencies are installed with Composer:

```powershell
composer install
```

The `vendor` directory is development-only and is not committed or deployed.
